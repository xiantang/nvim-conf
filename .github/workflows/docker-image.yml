name: Docker Image CI

on:
  push:
    branches: [ "master","put_into_docker" ]
  pull_request:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag neovim
    - name: run the Docker image
      run: |
        docker run -dt  --name neovim  neovim tail -f /dev/null 
        docker exec -it neovim /bin/bash -c "nvim --headless --startuptime log +qa"
        docker exec -it neovim /bin/bash -c "cat log" | tail -n 1 |awk '{print $1}'
